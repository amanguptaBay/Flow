/**
 * Feature flags — flip these to enable/disable features at runtime.
 *
 * timeline_scrub  — allows dragging the timeline slider freely.
 *                   When false, users are limited to Prev / Next / Play.
 */
export const FLAGS = {
    timeline_scrub: false,
};
